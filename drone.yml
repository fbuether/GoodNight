
kind: pipeline
type: docker
name: default

# trigger:
#   branch:
#     - release
#   event:
#     - tag

steps:
  - name: build
    image: plugins/docker
    settings:
      repo: goodnight
      dry_run: true
      auto_tag: true
