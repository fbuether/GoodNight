
kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: plugins/docker
    settings:
      repo: goodnight
      dry_run: true
      auto_tag: true
      purge: false
      debug: true
      launch_debug: true
